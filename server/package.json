{
  "name": "rock-generator-server",
  "version": "0.0.1",
  "description": "Procedural generation server which can be used to generate rocks and create fbx files.",
  "main": "./src/index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Henri Iltanen",
  "license": "ISC",
  "dependencies": {
    "bindings": "~1.2.1",
    "node-addon-api": "*"
  },
  "gypfile": true
}
